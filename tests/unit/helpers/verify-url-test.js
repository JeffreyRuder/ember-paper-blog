import { verifyUrl } from 'blog/helpers/verify-url';
import { module, test } from 'qunit';

module('Unit | Helper | verify url');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = verifyUrl([42]);
  assert.ok(result);
});
